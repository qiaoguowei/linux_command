#include <stddef.h>

int main() {

    //bool
    bool a = false;
    if (!a) {

    } else if (a) {

    }

    //int
    int b = 0;
    if (b == 0) {

    }

    //float
    const float EXP = 0.000001;
    float c;
    if (c < EXP && c > -EXP) {

    }

    //pointer
    char *d;
    if (d != NULL) {

    }

}